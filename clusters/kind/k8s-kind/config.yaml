# Kind local cluster (committed). Directory name is the config id.
# Kind cluster name matches cluster_name. Outputs: sensitive/kind/<cluster_name>/
#
#   ./scripts/infra/up.sh kind
#   ./scripts/infra/up.sh kind k8s-kind

cluster_name: k8s-kind

terraform:
  kubernetes_version: "1.32.2"
  control_plane_nodes: 1
  # Control-plane only for the laptop lab.
  worker_nodes: 0
  # Host ports for ingress (Kind maps these to container 80/443).
  http_host_port: 8080
  https_host_port: 8443
