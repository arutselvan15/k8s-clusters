.kube/
.idea

# Provider cache next to the .tf files (not in sensitive/)
**/.terraform/

# Secrets + Terraform outputs (S3). Keep README and *.example.
sensitive/**
!sensitive/README.md
!sensitive/aws/
!sensitive/aws/*.example
!sensitive/openstack/
!sensitive/openstack/*.example
