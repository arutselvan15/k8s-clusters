.kube/
bootstrap/env/bootstrap.env
.idea

# Terraform runtime (keep *.tf and STEPS.md tracked)
**/.terraform/
*.tfstate
*.tfstate.*
.terraform.tfstate.lock.info
crash.log
*.tfplan
terraform.tfvars

# Cloud secrets and generated keys (never commit)
.aws/credentials
.aws/config
.aws/*.pem
.aws/known_hosts
.openstack/clouds.yaml
.openstack/config
.openstack/*.pem
