# Optional Helm override. Copy to sensitive/bootstrap/values.yaml (gitignored).
#
#   mkdir -p sensitive/bootstrap
#   cp bootstrap/argocd/values.example.yaml sensitive/bootstrap/values.yaml
#
# Merged after bootstrap/argocd/values.yaml. Do not put tokens or passwords here
# (use sensitive/bootstrap/bootstrap.env).

# global:
#   domain: argocd.example.com
# configs:
#   params:
#     url: https://argocd.example.com
# server:
#   ingress:
#     hostname: argocd.example.com
