# Copy to credentials (gitignored). Never commit real keys.
#   cp .aws/credentials.example .aws/credentials
#   chmod 600 .aws/credentials
#
# ./scripts/infra/up.sh aws uses this file via AWS_SHARED_CREDENTIALS_FILE.

[default]
aws_access_key_id = REPLACE_ME
aws_secret_access_key = REPLACE_ME
